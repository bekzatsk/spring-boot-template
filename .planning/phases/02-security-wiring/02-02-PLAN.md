---
phase: 02-security-wiring
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/main/kotlin/kz/innlab/template/authentication/JwtTokenService.kt
  - src/main/kotlin/kz/innlab/template/authentication/dto/AuthRequest.kt
  - src/main/kotlin/kz/innlab/template/user/UserController.kt
autonomous: true
requirements:
  - TOKN-01
  - SECU-08

must_haves:
  truths:
    - "JwtTokenService mints RS256-signed access tokens with 15-minute expiry using the NimbusJwtEncoder from Phase 1"
    - "A request with a valid RS256 Bearer token (signed by JwtTokenService using the loaded keystore) passes authentication and reaches the stub controller"
    - "A request to GET /api/v1/users/me without a Bearer token returns 401 with { error, message, status } JSON"
    - "Jakarta Validation annotations on auth request DTOs reject malformed input with 400 { error, message, status } JSON"
  artifacts:
    - path: "src/main/kotlin/kz/innlab/template/authentication/JwtTokenService.kt"
      provides: "RS256 JWT access token minting with 15-minute expiry"
      contains: "JwtEncoder"
    - path: "src/main/kotlin/kz/innlab/template/authentication/dto/AuthRequest.kt"
      provides: "Auth request DTO with Jakarta Validation annotations"
      contains: "NotBlank"
    - path: "src/main/kotlin/kz/innlab/template/user/UserController.kt"
      provides: "Stub GET /api/v1/users/me returning JWT claims to prove auth works"
      contains: "GetMapping"
  key_links:
    - from: "JwtTokenService.kt"
      to: "JwtEncoder bean (RsaKeyConfig)"
      via: "Constructor injection"
      pattern: "jwtEncoder.encode"
    - from: "UserController.kt"
      to: "SecurityFilterChain (SecurityConfig)"
      via: "OAuth2 resource server JWT authentication"
      pattern: "authentication.principal as Jwt"
    - from: "AuthRequest.kt"
      to: "GlobalExceptionHandler (Plan 01)"
      via: "@Valid on @RequestBody triggers MethodArgumentNotValidException caught by handler"
      pattern: "NotBlank"
---

<objective>
Create the JWT token minting service, auth request DTOs with validation, and a stub user controller to prove the security chain works end-to-end.

Purpose: TOKN-01 requires RS256-signed access tokens with 15-minute expiry. SECU-08 requires Jakarta Validation on auth request DTOs. The stub UserController verifies the full security chain: missing token returns 401, valid token reaches the controller, and the response proves no database was hit (SECU-02). This plan completes all Phase 2 success criteria.

Output: JwtTokenService, AuthRequest DTO, stub UserController, end-to-end verification
</objective>

<execution_context>
@/Users/bekzat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekzat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-security-wiring/2-RESEARCH.md
@.planning/phases/02-security-wiring/02-01-SUMMARY.md
@src/main/kotlin/kz/innlab/template/config/RsaKeyConfig.kt
@src/main/kotlin/kz/innlab/template/user/Role.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create JwtTokenService, auth request DTO, and stub UserController</name>
  <files>
    src/main/kotlin/kz/innlab/template/authentication/JwtTokenService.kt
    src/main/kotlin/kz/innlab/template/authentication/dto/AuthRequest.kt
    src/main/kotlin/kz/innlab/template/user/UserController.kt
  </files>
  <action>
Create 3 new Kotlin files:

**1. `authentication/JwtTokenService.kt`** — Package: `kz.innlab.template.authentication`. Annotated `@Service`. Constructor-inject `jwtEncoder: JwtEncoder` (the `NimbusJwtEncoder` bean from `RsaKeyConfig`).

Method `generateAccessToken(userId: UUID, roles: Set<Role>): String`:
- `val now = Instant.now()`
- Build `JwtClaimsSet` with:
  - `.issuer("template-app")`
  - `.issuedAt(now)`
  - `.expiresAt(now.plus(15, ChronoUnit.MINUTES))` — TOKN-01: 15-minute expiry
  - `.subject(userId.toString())` — sub = user UUID string
  - `.claim("roles", roles.map { it.name })` — e.g., ["USER"], must match JwtGrantedAuthoritiesConverter config in SecurityConfig
- Return `jwtEncoder.encode(JwtEncoderParameters.from(claims)).tokenValue`
- Do NOT set JwsHeader explicitly — Nimbus defaults to RS256 when JWKSource contains an RSA key (per research Pitfall 6)

Import `kz.innlab.template.user.Role`, `org.springframework.security.oauth2.jwt.JwtClaimsSet`, `org.springframework.security.oauth2.jwt.JwtEncoder`, `org.springframework.security.oauth2.jwt.JwtEncoderParameters`, `java.time.Instant`, `java.time.temporal.ChronoUnit`, `java.util.UUID`.

**2. `authentication/dto/AuthRequest.kt`** — Package: `kz.innlab.template.authentication.dto`. A data class for social login auth requests (used by Phase 3 Google auth controller and Phase 4 Apple auth controller).

```kotlin
data class AuthRequest(
    @field:NotBlank(message = "ID token is required")
    val idToken: String = ""
)
```

Import `jakarta.validation.constraints.NotBlank`. The `= ""` default is required for Kotlin data classes with `@field:` annotations to work with Jackson deserialization (Jackson needs a no-arg constructor or default values; Kotlin data classes need explicit defaults for Jackson to populate via property setting). SECU-08: the `@NotBlank` annotation ensures a blank/missing `idToken` triggers `MethodArgumentNotValidException` when used with `@Valid @RequestBody` in a controller.

**3. `user/UserController.kt`** — Package: `kz.innlab.template.user`. Annotated `@RestController`, `@RequestMapping("/api/v1/users")`.

Method `getCurrentUser(authentication: Authentication): Map<String, Any?>` annotated `@GetMapping("/me")`:
- Cast `authentication.principal` to `org.springframework.security.oauth2.jwt.Jwt`
- Return `mapOf("sub" to jwt.subject, "roles" to jwt.getClaimAsStringList("roles"))`
- This is a STUB — Phase 3 will replace with actual UserService lookup. The stub proves SECU-02: auth passes without hitting the database, and the JWT claims are accessible.

Import `org.springframework.security.core.Authentication`, `org.springframework.security.oauth2.jwt.Jwt`.
  </action>
  <verify>
Run `mvn compile -f /Users/bekzat/Workspace/Template/spring-boot-template/pom.xml` — must compile with zero errors. Verify all 3 new files exist.
  </verify>
  <done>
JwtTokenService mints RS256 access tokens with 15-minute expiry. AuthRequest DTO has @NotBlank validation on idToken. Stub UserController returns JWT claims from /api/v1/users/me without database access.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify end-to-end security chain with application startup and HTTP tests</name>
  <files></files>
  <action>
This task verifies all Phase 2 success criteria by starting the application and making HTTP requests. No files are created or modified.

**Step 1: Start the application**
- Ensure Docker is running: `docker compose -f /Users/bekzat/Workspace/Template/spring-boot-template/docker-compose.yml up -d`
- Start the app: `SPRING_PROFILES_ACTIVE=dev mvn spring-boot:run -f /Users/bekzat/Workspace/Template/spring-boot-template/pom.xml` (run in background, wait for "Started TemplateApplicationKt" in output)

**Step 2: Test 401 without Bearer token (Success Criterion 1)**
- `curl -s http://localhost:7070/api/v1/users/me`
- Expected: HTTP 401 with body `{"error":"Unauthorized","message":"Authentication required","status":401}`

**Step 3: Test CORS preflight (Success Criterion 2)**
- `curl -s -X OPTIONS -H "Origin: http://localhost:3000" -H "Access-Control-Request-Method: GET" -H "Access-Control-Request-Headers: Authorization" http://localhost:7070/api/v1/users/me -D -`
- Expected: HTTP 200 with `Access-Control-Allow-Origin: http://localhost:3000` header

**Step 4: Generate a valid test token and test 200 (Success Criterion 3)**
- Write a small test class or use the running app's JwtTokenService programmatically. Alternative: use `mvn test` with a `@SpringBootTest` + `MockMvc` test that auto-wires `JwtTokenService`, generates a token, and calls `/api/v1/users/me` with it.
- If manual: Create a minimal `@SpringBootTest` integration test at `src/test/kotlin/kz/innlab/template/SecurityIntegrationTest.kt` that:
  1. `@Autowired` injects `JwtTokenService`
  2. `@Autowired` injects `MockMvc` (via `@AutoConfigureMockMvc`)
  3. Generates a token: `jwtTokenService.generateAccessToken(UUID.randomUUID(), setOf(Role.USER))`
  4. Calls `mockMvc.perform(get("/api/v1/users/me").header("Authorization", "Bearer $token")).andExpect(status().isOk)`
  5. Also tests no-token returns 401: `mockMvc.perform(get("/api/v1/users/me")).andExpect(status().isUnauthorized)`
- Run: `mvn test -f /Users/bekzat/Workspace/Template/spring-boot-template/pom.xml`
- Expected: All tests pass

**Step 5: Stop the app after verification**

If the integration test approach is used (recommended), Steps 1-3 can be skipped since MockMvc tests the same chain without needing a running server. But if time permits, also test with curl against the running app to double-verify.

Note: The test file is a verification artifact, not a Phase 2 deliverable. It exists to prove the success criteria but should be kept as it will be useful in Phase 5 (Hardening).
  </action>
  <verify>
`mvn test` passes with all security integration tests green. Alternatively, curl commands against running app confirm: 401 without token, 200 with valid token, CORS headers on OPTIONS.
  </verify>
  <done>
Phase 2 success criteria verified: (1) GET /api/v1/users/me without token returns 401 with JSON body; (2) OPTIONS preflight returns 200 with CORS headers; (3) Valid RS256 Bearer token passes authentication and reaches controller without DB hit; (4) CORS origins are configurable via application.yaml.
  </done>
</task>

</tasks>

<verification>
1. `mvn compile` succeeds with zero errors
2. `mvn test` passes — integration tests verify 401 without token, 200 with valid token
3. JwtTokenService mints tokens with 15-minute expiry and "roles" claim
4. AuthRequest DTO has @NotBlank on idToken field
5. Stub UserController returns JWT claims (sub, roles) without database access
6. All Phase 2 success criteria from ROADMAP are satisfied
</verification>

<success_criteria>
- JwtTokenService produces RS256 access tokens with 15-minute expiry (TOKN-01)
- AuthRequest DTO rejects blank idToken with 400 via Jakarta Validation (SECU-08)
- GET /api/v1/users/me with valid token returns 200 with JWT claims (proves SECU-02: no DB hit)
- GET /api/v1/users/me without token returns 401 with { error, message, status } JSON
- Integration test proves the full security chain works
</success_criteria>

<output>
After completion, create `.planning/phases/02-security-wiring/02-02-SUMMARY.md`
</output>
