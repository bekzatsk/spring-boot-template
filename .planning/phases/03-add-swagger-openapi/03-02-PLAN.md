---
phase: 03-add-swagger-openapi
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/main/kotlin/kz/innlab/template/authentication/controller/AuthController.kt
  - src/main/kotlin/kz/innlab/template/authentication/controller/AccountManagementController.kt
  - src/main/kotlin/kz/innlab/template/user/controller/UserController.kt
  - src/main/kotlin/kz/innlab/template/notification/controller/NotificationController.kt
  - src/main/kotlin/kz/innlab/template/notification/controller/MailController.kt
  - src/main/kotlin/kz/innlab/template/notification/controller/TopicAdminController.kt
autonomous: true
requirements:
  - SWAGGER-04
  - SWAGGER-05

must_haves:
  truths:
    - "AuthController endpoints in Swagger UI show NO lock icon (public, no security requirement)"
    - "All @AuthenticationPrincipal Jwt parameters are hidden from generated spec (no 'jwt' parameter visible)"
    - "Each controller group has a descriptive @Tag in Swagger UI"
    - "Application starts and Swagger UI renders all endpoints correctly"
  artifacts:
    - path: "src/main/kotlin/kz/innlab/template/authentication/controller/AuthController.kt"
      provides: "Public auth endpoints marked with @Operation(security = []) and @Tag"
      contains: "@Tag"
    - path: "src/main/kotlin/kz/innlab/template/authentication/controller/AccountManagementController.kt"
      provides: "@Parameter(hidden = true) on all 5 @AuthenticationPrincipal Jwt params"
      contains: "@Parameter(hidden = true)"
    - path: "src/main/kotlin/kz/innlab/template/user/controller/UserController.kt"
      provides: "@Parameter(hidden = true) on @AuthenticationPrincipal Jwt param"
      contains: "@Parameter(hidden = true)"
    - path: "src/main/kotlin/kz/innlab/template/notification/controller/NotificationController.kt"
      provides: "@Parameter(hidden = true) on all 12 @AuthenticationPrincipal Jwt params"
      contains: "@Parameter(hidden = true)"
    - path: "src/main/kotlin/kz/innlab/template/notification/controller/MailController.kt"
      provides: "@Parameter(hidden = true) on all 7 @AuthenticationPrincipal Jwt params"
      contains: "@Parameter(hidden = true)"
    - path: "src/main/kotlin/kz/innlab/template/notification/controller/TopicAdminController.kt"
      provides: "@Tag annotation for admin topics group"
      contains: "@Tag"
  key_links:
    - from: "AuthController.kt"
      to: "Swagger UI"
      via: "@Operation(security = []) overrides global bearerAuth"
      pattern: "security = \\[\\]"
    - from: "All controllers"
      to: "Swagger UI"
      via: "@Parameter(hidden = true) hides jwt from spec"
      pattern: "@Parameter\\(hidden = true\\)"
---

<objective>
Annotate all 6 controllers for clean Swagger UI output: add @Tag for grouping, @Parameter(hidden = true) on all @AuthenticationPrincipal Jwt parameters, and @Operation(security = []) on public AuthController endpoints.

Purpose: Ensure generated API docs are clean -- no spurious 'jwt' parameters, public auth endpoints don't show lock icon, and endpoints are grouped by functional area.
Output: All controllers properly annotated, Swagger UI renders clean documentation.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-add-swagger-openapi/03-RESEARCH.md
@.planning/phases/03-add-swagger-openapi/03-01-SUMMARY.md
</context>

<interfaces>
<!-- Key imports needed for swagger annotations in controllers -->

From io.swagger.v3.oas.annotations:
```kotlin
import io.swagger.v3.oas.annotations.Operation
import io.swagger.v3.oas.annotations.Parameter
import io.swagger.v3.oas.annotations.tags.Tag
```

Usage patterns:
```kotlin
// On controller class: grouping
@Tag(name = "Authentication", description = "Public auth endpoints - no JWT required")

// On public endpoints: override global security
@Operation(summary = "...", security = [])

// On @AuthenticationPrincipal params: hide from spec
@Parameter(hidden = true) @AuthenticationPrincipal jwt: Jwt
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Annotate AuthController with @Tag and @Operation(security = []) for public endpoints</name>
  <files>
    src/main/kotlin/kz/innlab/template/authentication/controller/AuthController.kt
  </files>
  <read_first>
    src/main/kotlin/kz/innlab/template/authentication/controller/AuthController.kt
  </read_first>
  <action>
Modify `AuthController.kt` to:

1. Add these imports at the top of the file (with the other imports):
```kotlin
import io.swagger.v3.oas.annotations.Operation
import io.swagger.v3.oas.annotations.tags.Tag
```

2. Add `@Tag` annotation on the class, between `@RestController` and `@RequestMapping`:
```kotlin
@RestController
@Tag(name = "Authentication", description = "Public auth endpoints - no JWT required")
@RequestMapping("/api/v1/auth")
```

3. Add `@Operation(security = [])` annotation on EACH of the 9 endpoint methods to override the global bearerAuth security requirement. The annotation goes directly before the `@PostMapping` annotation. Here are ALL 9 methods to annotate:

- `googleLogin`: `@Operation(summary = "Authenticate with Google ID token", security = [])`
- `appleLogin`: `@Operation(summary = "Authenticate with Apple ID token", security = [])`
- `localRegister`: `@Operation(summary = "Register with email and password", security = [])`
- `localLogin`: `@Operation(summary = "Login with email and password", security = [])`
- `requestPhoneOtp`: `@Operation(summary = "Request SMS OTP code for phone authentication", security = [])`
- `verifyPhoneOtp`: `@Operation(summary = "Verify SMS OTP code and authenticate", security = [])`
- `refresh`: `@Operation(summary = "Refresh access token using refresh token", security = [])`
- `revoke`: `@Operation(summary = "Revoke a refresh token", security = [])`
- `forgotPassword`: `@Operation(summary = "Request password reset verification code", security = [])`
- `resetPassword`: `@Operation(summary = "Reset password with verification code", security = [])`

Note: AuthController has NO `@AuthenticationPrincipal Jwt` parameters, so no `@Parameter(hidden = true)` needed here.
  </action>
  <verify>
    <automated>cd /Users/bekzat/Workspace/Template/spring-boot-template && grep -c "@Tag" src/main/kotlin/kz/innlab/template/authentication/controller/AuthController.kt && grep -c "security = \[\]" src/main/kotlin/kz/innlab/template/authentication/controller/AuthController.kt && grep -c "@Operation" src/main/kotlin/kz/innlab/template/authentication/controller/AuthController.kt</automated>
  </verify>
  <acceptance_criteria>
    - AuthController.kt contains `@Tag(name = "Authentication"` (1 occurrence)
    - AuthController.kt contains `security = []` (10 occurrences, one per endpoint method)
    - AuthController.kt contains `import io.swagger.v3.oas.annotations.Operation`
    - AuthController.kt contains `import io.swagger.v3.oas.annotations.tags.Tag`
    - Each @Operation has a `summary` string and `security = []`
  </acceptance_criteria>
  <done>AuthController has @Tag for grouping and all 10 endpoints have @Operation(security = []) to suppress the lock icon in Swagger UI</done>
</task>

<task type="auto">
  <name>Task 2: Add @Parameter(hidden = true) and @Tag to all authenticated controllers</name>
  <files>
    src/main/kotlin/kz/innlab/template/authentication/controller/AccountManagementController.kt,
    src/main/kotlin/kz/innlab/template/user/controller/UserController.kt,
    src/main/kotlin/kz/innlab/template/notification/controller/NotificationController.kt,
    src/main/kotlin/kz/innlab/template/notification/controller/MailController.kt,
    src/main/kotlin/kz/innlab/template/notification/controller/TopicAdminController.kt
  </files>
  <read_first>
    src/main/kotlin/kz/innlab/template/authentication/controller/AccountManagementController.kt,
    src/main/kotlin/kz/innlab/template/user/controller/UserController.kt,
    src/main/kotlin/kz/innlab/template/notification/controller/NotificationController.kt,
    src/main/kotlin/kz/innlab/template/notification/controller/MailController.kt,
    src/main/kotlin/kz/innlab/template/notification/controller/TopicAdminController.kt
  </read_first>
  <action>
For each of the 5 controllers below, add the `@Tag` annotation on the class and `@Parameter(hidden = true)` on every `@AuthenticationPrincipal Jwt` parameter.

**Required import for `@Parameter` (add to each file that has `@AuthenticationPrincipal`):**
```kotlin
import io.swagger.v3.oas.annotations.Parameter
```

**Required import for `@Tag` (add to ALL 5 files):**
```kotlin
import io.swagger.v3.oas.annotations.tags.Tag
```

---

**A) AccountManagementController.kt** (5 `@AuthenticationPrincipal` params)

Add `@Tag` between `@RestController` and `@RequestMapping`:
```kotlin
@RestController
@Tag(name = "Account Management", description = "Change password, email, and phone for authenticated users")
@RequestMapping("/api/v1/users/me")
```

Add `@Parameter(hidden = true)` before EVERY `@AuthenticationPrincipal jwt: Jwt` in these 5 methods:
- `changePassword` (line 27)
- `requestChangeEmail` (line 39)
- `verifyChangeEmail` (line 51)
- `requestChangePhone` (line 63)
- `verifyChangePhone` (line 75)

Example transformation:
```kotlin
// BEFORE:
fun changePassword(
    @AuthenticationPrincipal jwt: Jwt,
// AFTER:
fun changePassword(
    @Parameter(hidden = true) @AuthenticationPrincipal jwt: Jwt,
```

---

**B) UserController.kt** (1 `@AuthenticationPrincipal` param)

Add `@Tag` between `@RestController` and `@RequestMapping`:
```kotlin
@RestController
@Tag(name = "Users", description = "User profile endpoints")
@RequestMapping("/api/v1/users")
```

Add `@Parameter(hidden = true)` to `getCurrentUser`:
```kotlin
fun getCurrentUser(@Parameter(hidden = true) @AuthenticationPrincipal jwt: Jwt): ResponseEntity<UserProfileResponse> {
```

---

**C) NotificationController.kt** (12 `@AuthenticationPrincipal` params)

Add `@Tag` between `@RestController` and `@RequestMapping`:
```kotlin
@RestController
@Tag(name = "Notifications", description = "Push notifications, device tokens, topics, preferences, and history")
@RequestMapping("/api/v1/notifications")
```

Add `@Parameter(hidden = true)` before EVERY `@AuthenticationPrincipal jwt: Jwt` in these 12 methods:
- `registerToken` (line 47)
- `listTokens` (line 56)
- `deleteToken` (line 65)
- `deleteAllTokens` (line 73)
- `sendToToken` (line 84)
- `sendMulticast` (line 95)
- `sendToTopic` (line 106)
- `subscribe` (line 121)
- `unsubscribe` (line 131)
- `getPreferences` (line 140)
- `updatePreferences` (line 154)
- `getHistory` (line 176)

---

**D) MailController.kt** (7 `@AuthenticationPrincipal` params)

Add `@Tag` between `@RestController` and `@RequestMapping`:
```kotlin
@RestController
@Tag(name = "Email", description = "Send emails via SMTP, read inbox via IMAP, mail history")
@RequestMapping("/api/v1/mail")
```

Add `@Parameter(hidden = true)` before EVERY `@AuthenticationPrincipal jwt: Jwt` in these 7 methods:
- `sendEmail` (line 42)
- `sendEmailWithAttachments` (line 59)
- `listInbox` (line 87)
- `getMessage` (line 103)
- `markRead` (line 112)
- `markUnread` (line 121)
- `getHistory` (line 133)

---

**E) TopicAdminController.kt** (0 `@AuthenticationPrincipal` params -- @Tag only)

Add `@Tag` between `@RestController` and `@RequestMapping`:
```kotlin
@RestController
@Tag(name = "Admin - Topics", description = "Topic management (requires ADMIN role)")
@RequestMapping("/api/v1/admin/topics")
```

Add import:
```kotlin
import io.swagger.v3.oas.annotations.tags.Tag
```

No `@Parameter(hidden = true)` needed -- this controller has no `@AuthenticationPrincipal` params.
  </action>
  <verify>
    <automated>cd /Users/bekzat/Workspace/Template/spring-boot-template && echo "AccountMgmt:" && grep -c "@Parameter(hidden = true)" src/main/kotlin/kz/innlab/template/authentication/controller/AccountManagementController.kt && echo "User:" && grep -c "@Parameter(hidden = true)" src/main/kotlin/kz/innlab/template/user/controller/UserController.kt && echo "Notification:" && grep -c "@Parameter(hidden = true)" src/main/kotlin/kz/innlab/template/notification/controller/NotificationController.kt && echo "Mail:" && grep -c "@Parameter(hidden = true)" src/main/kotlin/kz/innlab/template/notification/controller/MailController.kt && echo "Tags:" && grep -rc "@Tag" src/main/kotlin/kz/innlab/template/*/controller/ src/main/kotlin/kz/innlab/template/notification/controller/</automated>
  </verify>
  <acceptance_criteria>
    - AccountManagementController.kt contains exactly 5 occurrences of `@Parameter(hidden = true)`
    - AccountManagementController.kt contains `@Tag(name = "Account Management"`
    - UserController.kt contains exactly 1 occurrence of `@Parameter(hidden = true)`
    - UserController.kt contains `@Tag(name = "Users"`
    - NotificationController.kt contains exactly 12 occurrences of `@Parameter(hidden = true)`
    - NotificationController.kt contains `@Tag(name = "Notifications"`
    - MailController.kt contains exactly 7 occurrences of `@Parameter(hidden = true)`
    - MailController.kt contains `@Tag(name = "Email"`
    - TopicAdminController.kt contains `@Tag(name = "Admin - Topics"`
    - All 5 files contain `import io.swagger.v3.oas.annotations.tags.Tag`
    - 4 files (excluding TopicAdminController) contain `import io.swagger.v3.oas.annotations.Parameter`
  </acceptance_criteria>
  <done>All 5 authenticated controllers have @Tag annotations and all 25 @AuthenticationPrincipal Jwt parameters have @Parameter(hidden = true)</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Compile the full project:
```bash
cd /Users/bekzat/Workspace/Template/spring-boot-template && ./mvnw compile -q
```

2. Run existing tests to ensure no regressions:
```bash
cd /Users/bekzat/Workspace/Template/spring-boot-template && ./mvnw test -q
```

3. Verify total @Parameter(hidden = true) count across all controllers is 25:
```bash
grep -rc "@Parameter(hidden = true)" src/main/kotlin/kz/innlab/template/*/controller/ src/main/kotlin/kz/innlab/template/notification/controller/
```

4. Verify all 6 controllers have @Tag:
```bash
grep -rc "@Tag" src/main/kotlin/kz/innlab/template/*/controller/ src/main/kotlin/kz/innlab/template/notification/controller/
```
</verification>

<success_criteria>
- `./mvnw compile` succeeds
- All existing tests pass
- AuthController has @Tag and 10 @Operation(security = []) annotations
- 25 total @Parameter(hidden = true) annotations across authenticated controllers
- All 6 controllers have @Tag annotations
- No @AuthenticationPrincipal Jwt parameter exists without @Parameter(hidden = true)
</success_criteria>

<output>
After completion, create `.planning/phases/03-add-swagger-openapi/03-02-SUMMARY.md`
</output>
