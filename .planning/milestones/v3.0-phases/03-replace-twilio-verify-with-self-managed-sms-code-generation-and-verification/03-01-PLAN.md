---
phase: 03-replace-twilio-verify-with-self-managed-sms-code-generation-and-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/kz/innlab/template/authentication/model/SmsVerification.kt
  - src/main/kotlin/kz/innlab/template/authentication/repository/SmsVerificationRepository.kt
  - src/main/kotlin/kz/innlab/template/authentication/service/SmsService.kt
  - src/main/kotlin/kz/innlab/template/authentication/service/ConsoleSmsService.kt
  - src/main/kotlin/kz/innlab/template/authentication/service/SmsVerificationService.kt
  - src/main/kotlin/kz/innlab/template/config/SmsSchedulerConfig.kt
  - src/main/resources/db/migration/V3__add_sms_verifications.sql
  - src/main/kotlin/kz/innlab/template/authentication/service/PhoneOtpService.kt
  - src/main/kotlin/kz/innlab/template/authentication/service/TwilioVerifyClient.kt
  - src/main/kotlin/kz/innlab/template/config/TwilioConfig.kt
  - pom.xml
  - src/main/resources/application.yaml
  - src/test/resources/application.yaml
autonomous: true
requirements:
  - SMS-01
  - SMS-02
  - SMS-03
  - SMS-04
  - SMS-05
  - SMS-06
  - SMS-08

must_haves:
  truths:
    - "6-digit OTP codes are generated using SecureRandom and BCrypt-hashed before storage"
    - "SmsVerification records are persisted in sms_verifications table with phone, code_hash, expires_at, used columns"
    - "SmsService interface exists with ConsoleSmsService registered conditionally via @ConditionalOnMissingBean"
    - "PhoneOtpService delegates to SmsVerificationService instead of TwilioVerifyClient"
    - "Twilio SDK dependency, TwilioConfig, and TwilioVerifyClient are completely removed"
    - "Expired and used verification records are cleaned up by a scheduled job every 10 minutes"
    - "Rate limiting enforced: max 1 OTP request per phone per 60 seconds"
    - "Max 3 verification attempts per code before invalidation"
    - "Codes expire after 5 minutes"
  artifacts:
    - path: "src/main/kotlin/kz/innlab/template/authentication/model/SmsVerification.kt"
      provides: "JPA entity for SMS verification records"
      contains: "class SmsVerification"
    - path: "src/main/kotlin/kz/innlab/template/authentication/repository/SmsVerificationRepository.kt"
      provides: "Repository with JPQL queries for active code lookup and bulk delete"
      contains: "interface SmsVerificationRepository"
    - path: "src/main/kotlin/kz/innlab/template/authentication/service/SmsService.kt"
      provides: "SMS sending interface"
      contains: "interface SmsService"
    - path: "src/main/kotlin/kz/innlab/template/authentication/service/ConsoleSmsService.kt"
      provides: "Dev/test SMS implementation that logs to stdout (no @Component — registered via @Bean @ConditionalOnMissingBean)"
      contains: "class ConsoleSmsService"
    - path: "src/main/kotlin/kz/innlab/template/authentication/service/SmsVerificationService.kt"
      provides: "Core OTP logic: generate, hash, persist, verify"
      contains: "class SmsVerificationService"
    - path: "src/main/kotlin/kz/innlab/template/config/SmsSchedulerConfig.kt"
      provides: "@EnableScheduling + cleanup job + @Bean @ConditionalOnMissingBean(SmsService) for ConsoleSmsService"
      contains: "@Scheduled"
    - path: "src/main/resources/db/migration/V3__add_sms_verifications.sql"
      provides: "sms_verifications table DDL"
      contains: "CREATE TABLE sms_verifications"
  key_links:
    - from: "SmsVerificationService"
      to: "SmsVerificationRepository"
      via: "constructor injection"
      pattern: "smsVerificationRepository"
    - from: "SmsVerificationService"
      to: "SmsService"
      via: "constructor injection"
      pattern: "smsService\\.sendCode"
    - from: "SmsVerificationService"
      to: "PasswordEncoder"
      via: "constructor injection"
      pattern: "passwordEncoder\\.(encode|matches)"
    - from: "PhoneOtpService"
      to: "SmsVerificationService"
      via: "constructor injection"
      pattern: "smsVerificationService\\.(sendCode|verifyCode)"
---

<objective>
Create the self-managed SMS verification infrastructure and wire it into PhoneOtpService, replacing Twilio Verify entirely.

Purpose: Eliminate external Twilio Verify dependency by implementing local OTP generation (SecureRandom), BCrypt-hashed storage (sms_verifications table), verification with attempt tracking, rate limiting, and scheduled cleanup. The existing `passwordEncoder` bean from LocalAuthConfig is reused for BCrypt hashing.

Output: New entity/repository/services for SMS verification, V3 Flyway migration, refactored PhoneOtpService, Twilio completely removed.
</objective>

<execution_context>
@/Users/bekzat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekzat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-replace-twilio-verify-with-self-managed-sms-code-generation-and-verification/03-RESEARCH.md
@src/main/kotlin/kz/innlab/template/authentication/model/RefreshToken.kt
@src/main/kotlin/kz/innlab/template/authentication/repository/RefreshTokenRepository.kt
@src/main/kotlin/kz/innlab/template/authentication/service/PhoneOtpService.kt
@src/main/kotlin/kz/innlab/template/authentication/service/TwilioVerifyClient.kt
@src/main/kotlin/kz/innlab/template/config/TwilioConfig.kt
@src/main/kotlin/kz/innlab/template/config/LocalAuthConfig.kt
@src/main/resources/application.yaml
@src/test/resources/application.yaml
@pom.xml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SMS verification infrastructure, refactor PhoneOtpService, and remove all Twilio artifacts</name>
  <files>
    src/main/kotlin/kz/innlab/template/authentication/model/SmsVerification.kt
    src/main/kotlin/kz/innlab/template/authentication/repository/SmsVerificationRepository.kt
    src/main/kotlin/kz/innlab/template/authentication/service/SmsService.kt
    src/main/kotlin/kz/innlab/template/authentication/service/ConsoleSmsService.kt
    src/main/kotlin/kz/innlab/template/authentication/service/SmsVerificationService.kt
    src/main/kotlin/kz/innlab/template/config/SmsSchedulerConfig.kt
    src/main/resources/db/migration/V3__add_sms_verifications.sql
    src/main/kotlin/kz/innlab/template/authentication/service/PhoneOtpService.kt
    src/main/kotlin/kz/innlab/template/authentication/service/TwilioVerifyClient.kt
    src/main/kotlin/kz/innlab/template/config/TwilioConfig.kt
    pom.xml
    src/main/resources/application.yaml
    src/test/resources/application.yaml
  </files>
  <action>
NOTE: This task touches 13 files but is a single atomic operation — the SMS verification infrastructure creation and Twilio removal are logically coupled and cannot compile independently. All files must be modified together for the project to compile.

**Part A — Create new SMS verification files (7 new files)**

**1. Create `SmsVerification.kt`** (mirrors RefreshToken entity pattern):
- JPA entity mapped to `sms_verifications` table
- Constructor params: `phone: String`, `codeHash: String`, `expiresAt: Instant`
- `@Id @GeneratedValue(strategy = GenerationType.UUID) val id: UUID? = null`
- `var used: Boolean = false` — marks consumed codes
- `var attempts: Int = 0` — tracks verification attempts (max 3)
- `@CreationTimestamp @Column(name = "created_at", nullable = false, updatable = false) val createdAt: Instant? = null`
- Manual `equals/hashCode` on `id` field only (same as RefreshToken/User pattern)
- `phone` is NOT unique — multiple rows can exist per phone until cleanup
- No FK to `users` table — phone user may not exist yet at OTP request time

**2. Create `SmsVerificationRepository.kt`**:
- Extends `JpaRepository<SmsVerification, UUID>`
- `findActiveByPhone(phone: String, now: Instant): SmsVerification?` — JPQL: `SELECT sv FROM SmsVerification sv WHERE sv.phone = :phone AND sv.used = false AND sv.expiresAt > :now AND sv.attempts < 3` (max 3 attempts)
- `deleteAllByPhone(phone: String)` — `@Modifying @Transactional @Query("DELETE FROM SmsVerification sv WHERE sv.phone = :phone")` — JPQL bulk delete (no N+1), same pattern as RefreshTokenRepository.deleteAllByUser
- `deleteExpiredOrUsed(cutoff: Instant)` — `@Modifying @Transactional @Query("DELETE FROM SmsVerification sv WHERE sv.expiresAt < :cutoff OR sv.used = true")` — for scheduled cleanup
- `existsByPhoneAndCreatedAtAfter(phone: String, since: Instant): Boolean` — for rate limiting (1 request per phone per 60 seconds)

**3. Create `SmsService.kt`** (interface):
- `fun sendCode(phone: String, code: String)` — single method interface
- No annotations on the interface itself

**4. Create `ConsoleSmsService.kt`**:
- Do NOT annotate with `@Component`. This class is a plain Kotlin class with no Spring annotations.
- Implements `SmsService`
- Log with SLF4J: `logger.info("[SMS] Sending code {} to {}", code, phone)`
- It will be registered as a bean conditionally via `@Bean @ConditionalOnMissingBean` in `SmsSchedulerConfig` (see step 6 below)

**5. Create `SmsVerificationService.kt`**:
- `@Service` with constructor injection of `SmsVerificationRepository`, `SmsService`, `PasswordEncoder`
- Companion object: `private const val CODE_BOUND = 1_000_000` (6-digit), `private const val EXPIRY_MINUTES = 5L`, `private const val RATE_LIMIT_SECONDS = 60L`, `private const val MAX_ATTEMPTS = 3`, `private val random = SecureRandom()`
- `@Transactional fun sendCode(phoneE164: String)`:
  1. Check rate limit: `if (smsVerificationRepository.existsByPhoneAndCreatedAtAfter(phoneE164, Instant.now().minusSeconds(RATE_LIMIT_SECONDS))) throw IllegalStateException("Please wait before requesting a new code")`
  2. Generate: `val code = String.format("%06d", random.nextInt(CODE_BOUND))`
  3. Hash: `val hash = passwordEncoder.encode(code)`
  4. Delete existing: `smsVerificationRepository.deleteAllByPhone(phoneE164)`
  5. Save: `smsVerificationRepository.save(SmsVerification(phone = phoneE164, codeHash = hash, expiresAt = Instant.now().plusSeconds(EXPIRY_MINUTES * 60)))`
  6. Send: `smsService.sendCode(phoneE164, code)`
- `@Transactional fun verifyCode(phoneE164: String, code: String): Boolean`:
  1. Find active: `val record = smsVerificationRepository.findActiveByPhone(phoneE164, Instant.now()) ?: return false`
  2. Increment attempts: `record.attempts++` then `smsVerificationRepository.save(record)`
  3. Match: `if (!passwordEncoder.matches(code, record.codeHash)) return false`
  4. Mark used: `record.used = true` then `smsVerificationRepository.save(record)`
  5. Return `true`

**6. Create `SmsSchedulerConfig.kt`**:
- `@Configuration @EnableScheduling`
- Constructor inject `SmsVerificationRepository`
- `@Scheduled(fixedRate = 600_000)` (10 minutes in ms) `@Transactional fun cleanupExpiredCodes()` — calls `smsVerificationRepository.deleteExpiredOrUsed(Instant.now())`
- Add companion object with SLF4J logger, log at debug level when cleanup runs
- **CRITICAL: Add `@Bean @ConditionalOnMissingBean(SmsService::class)` factory method:**
  ```kotlin
  @Bean
  @ConditionalOnMissingBean(SmsService::class)
  fun smsService(): SmsService = ConsoleSmsService()
  ```
  This registers ConsoleSmsService as the default SmsService bean ONLY when no other SmsService bean exists. When a real SMS provider is added later, it defines its own `SmsService` `@Bean` and ConsoleSmsService is automatically bypassed.

**7. Create `V3__add_sms_verifications.sql`**:
```sql
CREATE TABLE sms_verifications (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    phone VARCHAR(30) NOT NULL,
    code_hash VARCHAR(255) NOT NULL,
    expires_at TIMESTAMPTZ NOT NULL,
    used BOOLEAN NOT NULL DEFAULT FALSE,
    attempts INTEGER NOT NULL DEFAULT 0,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_sms_verifications_phone ON sms_verifications(phone);
```

**Part B — Refactor PhoneOtpService and remove Twilio (6 existing files)**

**8. Rewrite `PhoneOtpService.kt`:**
- Remove constructor params: `twilioVerifyClient: TwilioVerifyClient`, `@Value("\${app.auth.twilio.verify-service-sid}") serviceSid: String`
- Add constructor param: `smsVerificationService: SmsVerificationService`
- Keep: `userService`, `tokenService`, `refreshTokenService`
- `sendOtp(rawPhone: String)`: call `normalizeToE164(rawPhone)` then `smsVerificationService.sendCode(phoneE164)` — the rate limit IllegalStateException from SmsVerificationService will propagate and be caught by AuthExceptionHandler (IllegalStateException -> 409 Conflict). Decision: use the existing IllegalStateException -> 409 pattern for now (consistent with other conflict responses); leave a TODO comment for a dedicated 429 response.
- `verifyOtp(rawPhone: String, code: String): AuthResponse`: call `normalizeToE164(rawPhone)`, then `smsVerificationService.verifyCode(phoneE164, code)`. If returns `false`, throw `BadCredentialsException("Invalid or expired OTP")`. Otherwise proceed with `findOrCreatePhoneUser`, token generation, and return `AuthResponse`.
- Remove all Twilio imports

**9. Delete `TwilioVerifyClient.kt`** entirely (both interface and DefaultTwilioVerifyClient class)

**10. Delete `TwilioConfig.kt`** entirely

**11. Update `pom.xml`:**
- Remove the `<dependency>` block for `com.twilio.sdk:twilio:11.3.3`

**12. Update `src/main/resources/application.yaml`:**
- Remove the entire `twilio:` section under `app.auth:` (account-sid, auth-token, verify-service-sid — all 3 keys)

**13. Update `src/test/resources/application.yaml`:**
- Remove the entire `twilio:` section under `app.auth:` (account-sid, auth-token, verify-service-sid — all 3 keys)
  </action>
  <verify>
Run `./mvnw compile -pl . -q` — must compile cleanly with zero errors. Verify: `grep -r "twilio\|Twilio" src/main/` returns zero results. Verify TwilioVerifyClient.kt and TwilioConfig.kt no longer exist on disk. Verify pom.xml has no twilio dependency. Verify application.yaml (both main and test) have no `twilio:` section. Verify all 7 new files exist. Verify SmsVerification has `attempts` field, repository has rate-limit query, SmsVerificationService has rate limit check and attempt tracking. Verify ConsoleSmsService has NO `@Component` annotation. Verify SmsSchedulerConfig has `@Bean @ConditionalOnMissingBean(SmsService::class) fun smsService()` method.
  </verify>
  <done>
SmsVerification entity with phone/codeHash/expiresAt/used/attempts fields exists. SmsVerificationRepository has findActiveByPhone (with attempts < 3 filter), deleteAllByPhone, deleteExpiredOrUsed, and existsByPhoneAndCreatedAtAfter queries. SmsService interface exists. ConsoleSmsService implementation exists (no @Component — registered conditionally via @Bean @ConditionalOnMissingBean in SmsSchedulerConfig). SmsVerificationService generates 6-digit codes with SecureRandom, BCrypt-hashes via existing passwordEncoder, enforces 60-second rate limit and 3-attempt max, 5-minute expiry. SmsSchedulerConfig runs cleanup every 10 minutes and registers ConsoleSmsService conditionally. V3 migration creates sms_verifications table with index on phone. PhoneOtpService uses SmsVerificationService (not Twilio). TwilioVerifyClient.kt and TwilioConfig.kt deleted. Twilio SDK removed from pom.xml. All twilio config removed from both application.yaml files. Project compiles cleanly with zero Twilio references.
  </done>
</task>

</tasks>

<verification>
1. `./mvnw compile -pl . -q` succeeds with zero errors
2. `grep -r "twilio\|Twilio" src/main/` returns no results
3. All 7 new files exist in the expected packages
4. TwilioVerifyClient.kt and TwilioConfig.kt no longer exist
5. V3 migration file exists with correct schema
6. SmsVerificationService uses `SecureRandom` (not `Random` or `ThreadLocalRandom`)
7. SmsVerificationService uses the injected `passwordEncoder` bean for hashing (not a custom encoder)
</verification>

<success_criteria>
- Self-managed SMS verification infrastructure fully replaces Twilio Verify
- 6-digit codes generated with SecureRandom and BCrypt-hashed before storage
- Rate limiting (1 per phone per 60s) and attempt tracking (max 3) enforced
- 5-minute code expiry with scheduled cleanup every 10 minutes
- PhoneOtpService delegates to SmsVerificationService instead of TwilioVerifyClient
- Zero Twilio references remain in the codebase
- Project compiles successfully
</success_criteria>

<output>
After completion, create `.planning/phases/03-replace-twilio-verify-with-self-managed-sms-code-generation-and-verification/03-01-SUMMARY.md`
</output>
