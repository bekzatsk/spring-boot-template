---
phase: 05-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .mvn/wrapper/maven-wrapper.properties
  - src/main/kotlin/kz/innlab/template/authentication/AuthController.kt
  - src/main/kotlin/kz/innlab/template/config/SecurityConfig.kt
  - src/test/resources/application.yaml
autonomous: true
requirements:
  - INFR-07
  - INFR-08

must_haves:
  truths:
    - "./mvnw clean package compiles and passes all tests with zero Maven-level warnings"
    - "./mvnw spring-boot:run starts successfully after docker-compose up -d"
    - "// TODO: rate limiting markers exist at all four AuthController endpoint methods"
    - "// TODO: rate limiting marker exists in SecurityConfig at the oauth2ResourceServer filter chain entry point"
    - "No Hibernate H2Dialect deprecation warning appears during test execution"
  artifacts:
    - path: ".mvn/wrapper/maven-wrapper.properties"
      provides: "Maven Wrapper configuration for clean-checkout ./mvnw support"
      contains: "distributionUrl"
    - path: "src/main/kotlin/kz/innlab/template/authentication/AuthController.kt"
      provides: "Rate limiting TODO markers at all auth endpoint methods"
      contains: "TODO: rate limiting"
    - path: "src/main/kotlin/kz/innlab/template/config/SecurityConfig.kt"
      provides: "Rate limiting TODO marker at filter chain insertion point"
      contains: "TODO: rate limiting"
    - path: "src/test/resources/application.yaml"
      provides: "Clean test config without deprecated dialect and with open-in-view disabled"
      contains: "open-in-view: false"
  key_links:
    - from: ".mvn/wrapper/maven-wrapper.properties"
      to: "mvnw"
      via: "mvnw reads distributionUrl from properties file at startup"
      pattern: "distributionUrl=.*apache-maven"
    - from: "src/test/resources/application.yaml"
      to: "Hibernate auto-detection"
      via: "Removing database-platform lets Hibernate auto-detect H2 dialect without deprecation warning"
      pattern: "open-in-view: false"
---

<objective>
Harden the template for clean-checkout developer experience: restore Maven Wrapper config so `./mvnw` works, add rate limiting TODO markers at all auth entry points, and eliminate test YAML deprecation warnings.

Purpose: INFR-07 (rate limiting markers) and INFR-08 (project compiles and runs from clean checkout) are the final two requirements before the template is complete.

Output: A template that a developer can clone, run `docker-compose up -d && ./mvnw spring-boot:run`, and immediately have a working auth backend — with clear TODO markers showing where to add rate limiting.
</objective>

<execution_context>
@/Users/bekzat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekzat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-hardening/5-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add rate limiting TODO markers and fix test YAML warnings</name>
  <files>
    src/main/kotlin/kz/innlab/template/authentication/AuthController.kt
    src/main/kotlin/kz/innlab/template/config/SecurityConfig.kt
    src/test/resources/application.yaml
  </files>
  <action>
1. Edit `AuthController.kt` — add a `// TODO: rate limiting` comment as the first line inside each of the four endpoint method bodies:
   - `googleLogin`: `// TODO: rate limiting — add per-IP or per-client rate limit here before delegating to service`
   - `appleLogin`: `// TODO: rate limiting — add per-IP or per-client rate limit here before delegating to service`
   - `refresh`: `// TODO: rate limiting — refresh endpoint is a common abuse target; consider per-IP limit`
   - `revoke`: `// TODO: rate limiting — optional; include in global auth rate limit policy`

2. Edit `SecurityConfig.kt` — add a rate limiting TODO comment inside the `oauth2ResourceServer { }` block, before the `jwt { }` block:
   ```
   // TODO: rate limiting — to rate-limit authenticated API requests, insert a custom
   // OncePerRequestFilter before BearerTokenAuthenticationFilter:
   //   http.addFilterBefore(myRateLimitFilter, BearerTokenAuthenticationFilter::class.java)
   ```

3. Edit `src/test/resources/application.yaml`:
   - Remove the `database-platform: org.hibernate.dialect.H2Dialect` line (Hibernate auto-detects H2; explicit setting triggers HHH90000025 deprecation warning)
   - Add `open-in-view: false` under the `jpa:` block (suppress Spring's open-in-view WARN that appears because test YAML overrides main YAML which already has this set)

Important: Do NOT create a `JwtAuthenticationFilter` class. This project uses Spring Security's built-in `BearerTokenAuthenticationFilter` via the oauth2ResourceServer DSL. The marker in SecurityConfig serves as the filter-chain insertion point documentation.
  </action>
  <verify>
    - `grep -r "TODO: rate limiting" src/main/kotlin/` returns 5 matches (4 in AuthController.kt, 1 block in SecurityConfig.kt)
    - `grep "database-platform" src/test/resources/application.yaml` returns no matches
    - `grep "open-in-view: false" src/test/resources/application.yaml` returns 1 match
  </verify>
  <done>All four AuthController endpoint methods have rate limiting TODO comments. SecurityConfig has a filter-chain insertion point TODO. Test YAML no longer specifies H2Dialect explicitly and has open-in-view disabled.</done>
</task>

<task type="auto">
  <name>Task 2: Create Maven Wrapper properties and verify full build</name>
  <files>
    .mvn/wrapper/maven-wrapper.properties
  </files>
  <action>
1. Create the directory `.mvn/wrapper/` if it doesn't exist.

2. Create `.mvn/wrapper/maven-wrapper.properties` with:
   ```properties
   distributionUrl=https://repo.maven.apache.org/maven2/org/apache/maven/apache-maven/3.9.9/apache-maven-3.9.9-bin.zip
   wrapperVersion=3.3.4
   ```
   Spring Boot 4.0.3 requires Maven 3.9+. Maven 3.9.9 is the latest 3.9.x.

3. Run `./mvnw clean package` to verify:
   - The Maven Wrapper downloads Maven 3.9.9 successfully
   - The project compiles with zero Maven `[WARNING]` lines (compiler-level)
   - All tests pass (existing 9 tests from phases 2-4)
   - No Hibernate `HHH90000025` deprecation warning in test output
   - No `spring.jpa.open-in-view is enabled by default` warning in test output

4. If the build fails, diagnose and fix. The JVM-level `WARNING:` lines from Maven's own Guice dependency (sun.misc.Unsafe) are NOT project warnings and should be ignored.

Important: Do NOT commit `maven-wrapper.jar` — modern Maven Wrapper 3.3+ does not need the jar. Only `maven-wrapper.properties` should be committed.
  </action>
  <verify>
    - `./mvnw clean package` exits with BUILD SUCCESS
    - `cat .mvn/wrapper/maven-wrapper.properties` shows distributionUrl with Maven 3.9.9
    - `./mvnw clean package 2>&1 | grep -c "HHH90000025"` returns 0
    - `./mvnw clean package 2>&1 | grep -c "open-in-view is enabled"` returns 0
  </verify>
  <done>Maven Wrapper properties file exists with Maven 3.9.9 distribution URL. `./mvnw clean package` succeeds with all tests passing, no Hibernate dialect deprecation warnings, and no open-in-view warnings.</done>
</task>

</tasks>

<verification>
1. `./mvnw clean package` — BUILD SUCCESS with all tests passing
2. `grep -rn "TODO: rate limiting" src/main/kotlin/` — 5+ matches across AuthController.kt and SecurityConfig.kt
3. `cat .mvn/wrapper/maven-wrapper.properties` — valid Maven 3.9.9 distribution URL
4. `grep "database-platform" src/test/resources/application.yaml` — no matches (removed)
5. `grep "open-in-view" src/test/resources/application.yaml` — shows `open-in-view: false`
</verification>

<success_criteria>
- INFR-07: Rate limiting TODO markers present at all 4 auth endpoint methods in AuthController.kt AND at the oauth2ResourceServer filter chain entry point in SecurityConfig.kt
- INFR-08: `./mvnw clean package` succeeds from the project root with zero test failures, and `.mvn/wrapper/maven-wrapper.properties` exists so `./mvnw` works on a clean checkout
- No Hibernate H2Dialect deprecation warning (HHH90000025) in test output
- No Spring open-in-view warning in test output
</success_criteria>

<output>
After completion, create `.planning/phases/05-hardening/05-01-SUMMARY.md`
</output>
